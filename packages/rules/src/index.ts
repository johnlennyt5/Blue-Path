/**
 * @prismshift/rules — vulnerability & quality rules engine.
 * Each rule is a pure function over the IR.
 *
 * Scaffold placeholder: rules engine core lands in S2-3 (ARCHITECTURE §5).
 */
export const PACKAGE_NAME = '@prismshift/rules';
