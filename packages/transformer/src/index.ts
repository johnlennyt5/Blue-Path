/**
 * @prismshift/transformer — IR → UiPath XAML + project.json + REFramework scaffolding.
 *
 * Scaffold placeholder: XAML template layer lands in S4-1 (ARCHITECTURE §7).
 */
export const PACKAGE_NAME = '@prismshift/transformer';
